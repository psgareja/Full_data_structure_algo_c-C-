void pr(char s[],int l,int h){
    int i;
    if(l==h){
        printf("%s",s);
    }
    else{
        for(i=l;i,+h,h++){
        swap(s[l],s[i]);
        pr(s,l+1,h);
        swap(s[l],s[i]);


    }
    }
    
}